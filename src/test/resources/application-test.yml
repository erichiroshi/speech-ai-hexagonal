spring:
  config:
    activate:
      on-profile: test

  flyway:
    enabled: false

speaches:
  base-url: http://localhost:9999
  model: Systran/faster-whisper-small

logging:
  level:
    com.erichiroshi: DEBUG
